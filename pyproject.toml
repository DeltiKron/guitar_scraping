[project]
name = "guitar-scraping"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    'matplotlib',
    'sqlalchemy',
    'pandas',
    'beautifulsoup4',
    "pip>=24.3.1",
    "ipykernel",
    'seaborn',
    "numpy>=2.2.1",
    "tqdm>=4.67.1",
    "dbt>=1.0.0.38.22",
    "dbt-sqlite>=1.5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
